diff --git a/node_modules/cm-chessboard/src/extensions/right-click-annotator/RightClickAnnotator.js b/node_modules/cm-chessboard/src/extensions/right-click-annotator/RightClickAnnotator.js
index 4100512..150421d 100644
--- a/node_modules/cm-chessboard/src/extensions/right-click-annotator/RightClickAnnotator.js
+++ b/node_modules/cm-chessboard/src/extensions/right-click-annotator/RightClickAnnotator.js
@@ -65,8 +65,8 @@ export class RightClickAnnotator extends Extension {
         })
 
         // register public API
-        this.chessboard.getAnnotations = this.getAnnotations.bind(this.chessboard)
-        this.chessboard.setAnnotations = this.setAnnotations.bind(this.chessboard)
+        this.chessboard.getAnnotations = this.getAnnotations.bind(this)
+        this.chessboard.setAnnotations = this.setAnnotations.bind(this)
     }
 
     getAnnotations() {
diff --git a/node_modules/cm-chessboard/src/view/VisualMoveInput.js b/node_modules/cm-chessboard/src/view/VisualMoveInput.js
index 1db6fdc..8d2717b 100644
--- a/node_modules/cm-chessboard/src/view/VisualMoveInput.js
+++ b/node_modules/cm-chessboard/src/view/VisualMoveInput.js
@@ -300,6 +300,11 @@ export class VisualMoveInput {
                             } else {
                                 this.setMoveInputState(MOVE_INPUT_STATE.reset)
                             }
+                        } else {
+                            // upstream bug: an accepted move onto a same-color
+                            // piece fell through with no state change, leaving
+                            // the input machine stuck — complete it as a move
+                            this.setMoveInputState(MOVE_INPUT_STATE.moveDone, {square: square})
                         }
                     } else {
                         this.setMoveInputState(MOVE_INPUT_STATE.moveDone, {square: square})
diff --git a/node_modules/cm-chessboard/src/view/ChessboardView.js b/node_modules/cm-chessboard/src/view/ChessboardView.js
--- a/node_modules/cm-chessboard/src/view/ChessboardView.js
+++ b/node_modules/cm-chessboard/src/view/ChessboardView.js
@@ -49,14 +49,14 @@ export class ChessboardView {
         if (chessboard.props.responsive) {
             if (typeof ResizeObserver !== "undefined") {
                 this.resizeObserver = new ResizeObserver(() => {
-                    // Defer via setTimeout to avoid "ResizeObserver loop
-                    // completed with undelivered notifications." The timeout
-                    // id is tracked so destroy() can cancel a pending call
-                    // and avoid running handleResize on a destroyed board.
-                    this.resizeTimeout = setTimeout(() => {
-                        this.resizeTimeout = null
-                        this.handleResize()
-                    })
+                    // Synchronous on purpose: ResizeObserver delivers before
+                    // paint, so redrawing here means a resized board never
+                    // paints a stale frame (a setTimeout defer did, and every
+                    // layout change around a board showed a one-frame jank).
+                    // handleResize no-ops when the size is unchanged, so the
+                    // follow-up notification this redraw provokes terminates
+                    // the loop instead of spinning it.
+                    this.handleResize()
                 })
                 this.resizeObserver.observe(this.chessboard.context)
             } else {
